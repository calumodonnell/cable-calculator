/*jslint browser:true*/
/*global $, jQuery, alert, angular, console, app*/

// pageCtrl controller
app.directive('setHeight', function () {
    "use strict";

    return function (scope, element, attrs) {
        //$('.sidebar').height() = $('.cable-content').outerHeight();
        console.log('test');
    };
});
